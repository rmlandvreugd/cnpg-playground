apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: verstappen-readonly
  namespace: rbr-ver-db
spec:
  refreshInterval: "15m"
  secretStoreRef:
    name: vault-approle-rbr
    kind: ClusterSecretStore
  target:
    name: verstappen-readonly
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: rbr/ver/readonly
        property: username
    - secretKey: password
      remoteRef:
        key: rbr/ver/readonly
        property: password

apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cnpg-custom-pg-rbr-ver
  namespace: grafana
spec:
  instanceSelector:
    matchLabels:
      dashboards: "grafana-rbr-ver"
  json: |
    {
      "title": "CNPG — Custom PostgreSQL Metrics",
      "uid": "cnpg-custom-pg-rbr-ver",
      "schemaVersion": 38,
      "version": 1,
      "refresh": "30s",
      "time": { "from": "now-1h", "to": "now" },
      "templating": {
        "list": [
          {
            "name": "datasource",
            "type": "datasource",
            "query": "prometheus",
            "label": "Datasource",
            "current": {},
            "hide": 0
          },
          {
            "name": "cluster",
            "type": "query",
            "datasource": { "type": "prometheus", "uid": "${datasource}" },
            "definition": "label_values(cnpg_collector_up, cluster)",
            "query": { "query": "label_values(cnpg_collector_up, cluster)", "refId": "StandardVariableQuery" },
            "label": "Cluster",
            "multi": true,
            "includeAll": true,
            "current": {}
          },
          {
            "name": "namespace",
            "type": "query",
            "datasource": { "type": "prometheus", "uid": "${datasource}" },
            "definition": "label_values(cnpg_collector_up{cluster=~\"$cluster\"}, namespace)",
            "query": { "query": "label_values(cnpg_collector_up{cluster=~\"$cluster\"}, namespace)", "refId": "StandardVariableQuery" },
            "label": "Namespace",
            "multi": true,
            "includeAll": true,
            "current": {}
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "Replication Lag by Instance",
          "type": "timeseries",
          "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "unit": "s",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "green", "value": null },
                  { "color": "yellow", "value": 5 },
                  { "color": "red", "value": 30 }
                ]
              }
            }
          },
          "targets": [
            {
              "expr": "cnpg_pg_replication_lag{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "{{pod}}",
              "refId": "A"
            }
          ]
        },
        {
          "id": 2,
          "title": "Connection State Breakdown",
          "type": "timeseries",
          "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": { "unit": "short" },
            "overrides": [
              { "matcher": { "id": "byName", "options": "active" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
              { "matcher": { "id": "byName", "options": "idle" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] },
              { "matcher": { "id": "byName", "options": "idle in transaction" }, "properties": [{ "id": "color", "value": { "fixedColor": "orange", "mode": "fixed" } }] }
            ]
          },
          "options": { "tooltip": { "mode": "multi" } },
          "targets": [
            {
              "expr": "sum by (state) (cnpg_backends_total{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"})",
              "legendFormat": "{{state}}",
              "refId": "A"
            }
          ]
        },
        {
          "id": 3,
          "title": "Database Size",
          "type": "timeseries",
          "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": { "defaults": { "unit": "bytes" } },
          "targets": [
            {
              "expr": "cnpg_pg_database_size_bytes{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "{{datname}}",
              "refId": "A"
            }
          ]
        },
        {
          "id": 4,
          "title": "Long-running Queries (>30s)",
          "type": "timeseries",
          "gridPos": { "x": 12, "y": 8, "w": 12, "h": 8 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": {
            "defaults": {
              "unit": "short",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "green", "value": null },
                  { "color": "yellow", "value": 1 },
                  { "color": "red", "value": 5 }
                ]
              }
            }
          },
          "targets": [
            {
              "expr": "cnpg_pg_long_running_queries_count{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "count ({{pod}})",
              "refId": "A"
            },
            {
              "expr": "cnpg_pg_long_running_queries_max_age_seconds{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "max age s ({{pod}})",
              "refId": "B"
            }
          ]
        },
        {
          "id": 5,
          "title": "pgBouncer Pool Utilization",
          "type": "timeseries",
          "gridPos": { "x": 0, "y": 16, "w": 24, "h": 8 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "fieldConfig": { "defaults": { "unit": "short" } },
          "targets": [
            {
              "expr": "cnpg_pgbouncer_pools_cl_active{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "active client ({{pod}} {{database}})",
              "refId": "A"
            },
            {
              "expr": "cnpg_pgbouncer_pools_cl_waiting{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "waiting client ({{pod}} {{database}})",
              "refId": "B"
            },
            {
              "expr": "cnpg_pgbouncer_pools_sv_active{namespace=~\"$namespace\", pod=~\"$cluster-[0-9]+\"}",
              "legendFormat": "active server ({{pod}} {{database}})",
              "refId": "C"
            }
          ]
        }
      ]
    }

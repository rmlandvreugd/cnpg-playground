apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pg-local-readonly
  namespace: demo-local-db
spec:
  refreshInterval: "15m"
  secretStoreRef:
    name: vault-approle
    kind: ClusterSecretStore
  target:
    name: pg-local-readonly
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: pg-local/readonly
        property: username
    - secretKey: password
      remoteRef:
        key: pg-local/readonly
        property: password

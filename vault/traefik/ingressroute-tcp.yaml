apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: vault
  namespace: vault
spec:
  entryPoints:
    - vault
  routes:
    - match: HostSNI(`*`)
      services:
        - name: vault
          port: 8200
  tls:
    passthrough: true

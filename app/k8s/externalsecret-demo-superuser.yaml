apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: demo-superuser
  namespace: demo-db
spec:
  refreshInterval: "5m"
  secretStoreRef:
    name: vault-approle
    kind: ClusterSecretStore
  target:
    name: demo-superuser
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: cnpg/demo/superuser
        property: username
    - secretKey: password
      remoteRef:
        key: cnpg/demo/superuser
        property: password
